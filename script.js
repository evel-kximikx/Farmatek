 // Ініціалізація каруселі
 document.addEventListener('DOMContentLoaded', function () {
    const carousel = document.querySelector('.carousel');
    if (carousel) {
        // Реалізуйте логіку для ручного налаштування каруселі
    }
});